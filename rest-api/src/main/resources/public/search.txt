<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Recherche de cours</title>
</head>
<body>
    <h1>Rechercher un cours</h1>
    <input type="text" id="courseID" placeholder="Ex: IFT2255">
    <button onclick="searchCourse()">Chercher</button>

    <script>
        function searchCourse() {
            const id = document.getElementById("courseId").value;

            //Appel à API
            fetch(`/course/${id}`)
                .then(response =>{
                    if(!response.ok) throw new Error("Cours introuvable");
                    return response.json();
                })
                
                .then(data =>{
                document.getElementById("result").textContent = JSON.stringify(data, null, 2);
                })
                .catch (err =>{
                    document.getElementById("result").textContent = err;
                });
        }
    </script>
</body>
</html>